import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.dingc.fursonaatelier",
  appName: "兽格造像馆",
  webDir: "out",
  server: {
    androidScheme: "https",
  },
};

export default config;
